source 'https://rubygems.org'

# Use GitHub Pages gem for compatibility with GitHub Pages (includes Jekyll and supported plugins)
gem 'github-pages', group: :jekyll_plugins

# Windows timezone data
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]

# If you prefer to use plain Jekyll without the GitHub Pages gem, replace above with:
# gem 'jekyll', '~> 4.2'
